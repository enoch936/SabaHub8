"use client"; import { Star } from 'lucide-react'; interface AnimatedStarRatingProps { rating: number; size?: 'sm' | 'md' | 'lg'; animated?: boolean; interactive?: boolean; onChange?: (rating: number) => void;
} const SIZE_MAP = { sm: 'w-3.5 h-3.5', md: 'w-5 h-5', lg: 'w-7 h-7' }; export function AnimatedStarRating({ rating, size = 'md', animated = true, interactive, onChange }: AnimatedStarRatingProps) { return ( <div className="flex items-center gap-0.5"> {[1, 2, 3, 4, 5].map((star) => ( <button key={star} type="button" disabled={!interactive} onClick={() => onChange?.(star)} className={`${interactive ? 'cursor-pointer' : 'cursor-default'}`} > <Star className={`${SIZE_MAP[size]} transition-colors ${ star <= rating ? 'text-yellow-400 fill-yellow-400' : 'text-gray-300' }`} /> </button> ))} </div> );
}
