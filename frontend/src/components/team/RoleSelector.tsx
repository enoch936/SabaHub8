"use client"; import type { TeamRole } from '@/lib/types'; interface RoleSelectorProps { value: TeamRole; onChange: (role: TeamRole) => void; disabled?: boolean;
} const ROLES: { value: TeamRole; label: string; description: string }[] = [ { value: 'ADMIN', label: 'Admin', description: 'Full access to all features' }, { value: 'RECRUITER', label: 'Recruiter', description: 'Post jobs, manage contracts, view analytics' }, { value: 'VIEWER', label: 'Viewer', description: 'Read-only access to all sections' },
]; export function RoleSelector({ value, onChange, disabled }: RoleSelectorProps) { return ( <select value={value} onChange={(e) => onChange(e.target.value as TeamRole)} disabled={disabled} className="px-3 py-2 rounded-lg border border-[var(--border)] bg-[var(--background)] text-sm focus:outline-none focus:ring-2 focus:ring-primary disabled:opacity-50" > {ROLES.map((r) => ( <option key={r.value} value={r.value} title={r.description}> {r.label} </option> ))} </select> );
}
